# Answers

The shape of `answers.md`, one per application, inside its job folder. Written by `apply`, question
by question as the form goes.

| question | answer | source |
|---|---|---|
| | | |

`source` names where the answer came from: `application-data.md § Reusable answers`, the letter, the
tailored resume, or the candidate.
